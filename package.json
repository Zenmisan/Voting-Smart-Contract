
 {
  "name": "voting-contract-viem",
  "version": "1.0.0",
  "description": "Voting contract with Hardhat + Viem",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "test:gas": "REPORT_GAS=true npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "npx hardhat run scripts/deploy.ts --network sepolia",
    "interact": "npx hardhat run scripts/interact.ts --network localhost",
    "scenarios": "npx hardhat run scripts/test-scenarios.ts --network localhost",
    "node": "npx hardhat node",
    "clean": "npx hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^2.0.0",
    "@nomicfoundation/hardhat-viem": "^2.0.0",
    "@types/chai": "^4.3.5",
    "@types/mocha": "^10.0.1",
    "@types/node": "^20.4.2",
    "chai": "^4.3.7",
    "hardhat": "^2.17.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "viem": "^2.0.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}









